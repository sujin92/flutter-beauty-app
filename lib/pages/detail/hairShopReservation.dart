import 'package:flutter/material.dart';

class HairShopReservation extends StatelessWidget {
  const HairShopReservation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
